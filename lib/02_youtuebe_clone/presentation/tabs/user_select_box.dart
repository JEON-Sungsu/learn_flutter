import 'package:flutter/material.dart';

class UserSelectBox extends StatelessWidget {
  const UserSelectBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
